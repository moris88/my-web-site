name: 'Qualità del codice e Build'

on:
  push:
    branches: 
      - '*'
      - '*/*'
      - '**'
      - '!main'

jobs:
  build:
    runs-on: ubuntu-latest
    name: Qualità del codice e Build
    steps:
      - uses: actions/checkout@v2
      - name: Setup node
        uses: actions/setup-node@v2
        with:
          node-version: '20'
      - name: 'Installazione dipendenze'
        run: yarn install
      - name: 'Check qualità codice'
        run: yarn lint
      - name: 'Build test'
        run: yarn build